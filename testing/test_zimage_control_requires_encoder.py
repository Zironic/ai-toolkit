import pytest
pytest.skip("Test removed: strict Z-Image policy forbids auto-encoding raw control images in predict_noise_zimage; pre-encoded latents or precomputed control_context must be provided.", allow_module_level=True)
