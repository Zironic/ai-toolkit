{"author":{"id":"fb9734b7331903226ef3f5df545b960dd5ac64d18b49faee11e8b8e0bd561c91"},"ops":[{"type":1,"timestamp":1783461233,"nonce":"SwIFU5gJUoYjbEkhoILtWkdlyHk=","title":"FP8 sampling closures pinned demoted weights on GPU: WDDM fallback freed nothing at 2000px","message":"Reported by user 2026-07-07: at 2000x2000 the mid-denoise OOM fallback looped demoting the entire model while freeing no memory. Root cause: _enable_fp8_sampling installs resident forwards that capture the GPU qdata/scale as closure constants (deliberate compile optimization; comment assumed weights stay resident for the context lifetime). demote_layer moved param.data to CPU but the closure view kept the GPU storage alive -- an untracked pin, so every demote freed 0 bytes while the loop reported plan-bytes as progress. Compare ComfyUI/comfy-aimdo (docs/comfyui_dynamic_vram_analysis.md): their pages carry explicit pin_counts and pressure eviction refuses pinned pages; our closure was an invisible pin. Fix: (1) _release_fp8_sampling_for restores the demoted layers' original forwards (dropping closures) inside _sampling_demote_largest_block, and marks them _memory_management_fp8_sampling so the streamed fp8 path takes over; (2) the OOM demote path now governs on MEASURED allocatable bytes (_torch_allocatable_bytes delta) with a futility escape to the full streamed transition; (3) guard and step-trim demote paths pass the live restore list too. Regression tests: tests/test_sampling_fp8_demote_frees.py (fix + negative control proving the closure pin). 2000px smoke validation pending.","files":null}]}