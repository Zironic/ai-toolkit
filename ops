{"author":{"id":"fb9734b7331903226ef3f5df545b960dd5ac64d18b49faee11e8b8e0bd561c91"},"ops":[{"type":3,"timestamp":1783507632,"nonce":"8XHK2xFzQn/ItBz0QOIB23xojok=","message":"Phase 2 (all slices A-E) landed 2026-07-08, 319/319 tests passing. A: MemoryManager._desired_pin_bytes_for_offload_ids single-sources auto/explicit pin sizing scoped to actually-streamed offload_ids; attach_smart_training no longer over-sizes from plan[model_bytes] when blocks stay resident. B: PinnedWeightArena.build accepts budget_bytes (order-respecting pin-vs-pageable cap); _build_pinned_arena passes the resolved budget minus already-committed bytes; per-layer attach runs with budget=0 when the arena is active (arena is sole pinner, zero cudaHostRegister calls, kills the pin/unpin/repin triple churn on first attach). C: dropped the stale 'and not reserve_pin_for_ingraph' gate on use_pinned_arena in inference_resident (ingraph sampling borrows arena packs now, not a double-commit). D: MemoryManager._destroy_pinned_arena for explicit teardown (params detached via clone BEFORE arena.release(), never called from detach/sampling boundaries -- only true model unload). E: enable_ingraph_sampling exposes _ingraph_sampling_borrowed_count/_owned_count diagnostics; both Krea2 smokes instrumented with arena/ledger snapshots across the sampling boundary + explicit teardown check at exit. New test files: test_desired_pin_bytes, test_smart_training_pin_scoping, test_pinned_arena_budget, test_pinned_arena_teardown, test_ingraph_sampling_borrow_counts, plus additions to test_pinned_arena_sampling_boundary. Ready for a real GPU training-run validation with --pinned-arena on both smokes.","files":null}]}