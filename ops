{"author":{"id":"fb9734b7331903226ef3f5df545b960dd5ac64d18b49faee11e8b8e0bd561c91"},"ops":[{"type":1,"timestamp":1783461451,"nonce":"cNV9UVTwWmW/juxRkGMTSmK2eS0=","title":"Sampling drops all 9.9 GiB weight pins for the whole phase (pageable streaming + pin churn)","message":"User observed pinned ledger 0.00 GiB mid-run (2000px smoke, 2026-07-07). By design today: inference_resident detaches the training layout (releasing its ~9.92 GiB cudaHostRegister weight pins) and re-attaches sampling streaming with pinned_weight_gib=0.0 whenever reserve_pin_for_ingraph=True, so the whole DXGI shared budget is reserved for ingraph packs (avoids double-commit). Costs: (1) the eager pass and any pre-compile sampling stream up to 128 layers from PAGEABLE host memory -- slow H2D -- while ~12 GiB pin headroom sits unused until packs are built; (2) every sampling boundary pays ~10 GiB unpin + repin (page-locking runs 0.6-2 GB/s on consumer Windows =\u003e seconds per sample event, visible as move=1.2s / restore=2.0s). Better: reserve only the PLANNED pack bytes (streamed blocks x pack size) plus floor, and keep weight pins for the remainder; or repoint packs so they reuse the already-pinned weight storage (repoint=True path) instead of zeroing all weight pins. Related: 763bb75 (resource already mapped), da1aae5 (pin manager authority).","files":null}]}